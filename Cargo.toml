[workspace]
resolver = "2"
members = [
    "common",
    "backend",
    "frontend",
]

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }