[workspace]
resolver = "2"
members = [
    "backend",
    "common",
    "frontend",
]

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }